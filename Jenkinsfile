pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'node -v'
                sh 'npm install'
                sh 'npm test'
            }
        }
    }
}

    // sh 'node -v'
    //                   sh 'apt-get install chromium-browser'
