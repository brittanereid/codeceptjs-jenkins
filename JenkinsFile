pipeline {
    agent {
        docker { image 'selenium/standalone-chrome' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'npm install'
                sh 'npm run test'
            }
        }
    }
}