pipeline {
    agent {
        docker { image 'tenjaa/java-chrome-nodejs:latest'}
    }
    stages {
        stage('Test') {
            steps {
            echo"Testing node"
                    sh "npm install"
                    sh "HEADLESS=true && npx codeceptjs run"
            }
        }
    }
}