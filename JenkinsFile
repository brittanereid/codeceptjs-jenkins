pipeline {
    agent {
        docker { image 'selenium/standalone-chrome' }
    }
    stages {
        stage('Test') {
            steps {
            echo"Testing node"
                nodejs("Nodejs"){
                    sh "npm install"
                    sh "HEADLESS=true && npx codeceptjs run"
                }
            }
        }
    }
}