pipeline {
    agent {
        dockerfile true
    }
        stage("Run Tests"){
            steps{
                    sh "npm install"
                    sh "npx codeceptjs run"
            }
        }
    }
}