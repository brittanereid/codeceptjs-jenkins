pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage("Run Tests"){
            steps{
                echo"Testing node"
                    sh "npm install"
                    sh "npx codeceptjs run"     
            }
        }
    }
}
