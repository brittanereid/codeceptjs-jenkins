pipeline {
    agent {
        docker { image 'atools/chrome-headless' }
    }
    stages {
        stage('Test') {
            steps {
            echo"Testing node"
                nodejs("Nodejs"){
                    sh "npm install"
                    sh "npx codeceptjs run"
                }
            }
        }
    }
}