pipeline {
    agent {
        docker { image 'selenium/standalone-chrome'}
    }
    stages {
        stage('Test') {
            steps {
            echo"Testing node"
                    sh "npm install"
                    sh "HEADLESS=true && npx codeceptjs run"
            }
        }
    }
}