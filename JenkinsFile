pipeline {
    agent {
        docker { image 'selenium/node-chrome' }
    }
    stages {
        stage('Test') {
            steps {
            echo"Testing node"
                nodejs("Nodejs"){
                    sh "npm install"
                    sh "HEADLESS=true && npx codeceptjs run"
                }
            }
        }
    }
}