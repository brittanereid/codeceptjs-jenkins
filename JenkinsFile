pipeline {
    agent {
        docker { image 'minddocdev/node-java-chrome-e2e' }
    }
    stages {
        stage('Test') {
            steps {
            echo"Testing node"
                nodejs("Nodejs"){
                    sh "npm install"
                    sh "HEADLESS=true && npx codeceptjs run"
                }
            }
        }
    }
}